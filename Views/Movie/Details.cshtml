@model MyLibrary.mvc.ViewModels.MovieDetailsViewModel

<article class="page">
    <section class="movie-detail-container">
        <div class="movie-detail">
            <span class="back-icon">
                <a asp-action="index"><span><i class="fa-regular fa-arrow-left-long"></i> Tillbaka</span></a>
            </span>
            <h2 class="movie-detail-header">@Model.Name </h2>
            <img src="@Model.ImageUrl"> 
            <div class="movie-detail-info">
                <span><strong>Tid</strong></span>
                <span>@Model.Lenght min</span>
                <span><strong>Släppt</strong></span>
                <span>@Model.ReleaseDate</span>
                <span><strong>Genre</strong></span>
                <span>@Model.GenreType</span>
                <span><strong>Sida</strong></span>
                <span>@Model.SiteName</span>
            </div>
            <h4 class="movie-description ">Beskrivning</h4>
            <div class="movie-description ">@Model.Description</div>
        </div>
    </section>
</article>